# Image Compressor

Tool to compress images with sharp.

This tool is meant to improve loading times of your app by compressing
the image files.

## Usage

Sharp must be installed by running:

```
npm i sharp
```

To call the script run:
```
node ./resources/scripts/compress-images.mjs ./resources/images ./resources/images/originals
```