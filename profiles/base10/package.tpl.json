{
    "name": "[[ $pkg?abbrev ]]",
    "version": "[[ $pkg?version ]]",
    "description": "[[ $label ]]",
    "license": "GPL-3.0",
    "dependencies": {
        "@teipublisher/pb-components": "[[ $dependencies?dependencies?('@teipublisher/pb-components') ]]",
        "@jinntec/fore": "[[ $dependencies?dependencies?('@jinntec/fore') ]]",
        "@picocss/pico": "[[ $dependencies?dependencies?('@picocss/pico') ]]"
    },
    "devDependencies": {
        "ajv": "[[ $dependencies?devDependencies?ajv ]]",
        "ajv-draft-04": "[[ $dependencies?devDependencies?('ajv-draft-04') ]]",
        "ajv-formats": "[[ $dependencies?devDependencies?('ajv-formats') ]]",
        "cypress": "[[ $dependencies?devDependencies?cypress ]]",
        "cypress-ajv-schema-validator": "[[ $dependencies?devDependencies?('cypress-ajv-schema-validator') ]]",
        "fflate": "[[ $dependencies?devDependencies?fflate ]]",
        "glob": "[[ $dependencies?devDependencies?glob ]]"
    },
    "scripts": {
        "test": "cypress run",
        "test:open": "cypress open"
    }
}