{
    "name": "[[ $pkg?abbrev ]]",
    "version": "[[ $pkg?version ]]",
    "description": "[[ $label ]]",
    "license": "GPL-3.0",
    "dependencies": {
        "@teipublisher/pb-components": "[% if exists($dependencies?effectiveVersions?('@teipublisher/pb-components')) %][[ $dependencies?effectiveVersions?('@teipublisher/pb-components') ]][% else %][[ $dependencies?dependencies?('@teipublisher/pb-components') ]][% endif %]",
        "@jinntec/fore": "[% if exists($dependencies?effectiveVersions?('@jinntec/fore')) %][[ $dependencies?effectiveVersions?('@jinntec/fore') ]][% else %][[ $dependencies?dependencies?('@jinntec/fore') ]][% endif %]",
        "@picocss/pico": "[% if exists($dependencies?effectiveVersions?('@picocss/pico')) %][[ $dependencies?effectiveVersions?('@picocss/pico') ]][% else %][[ $dependencies?dependencies?('@picocss/pico') ]][% endif %]"
    },
    "devDependencies": {
        "ajv": "[[ $dependencies?devDependencies?ajv ]]",
        "ajv-draft-04": "[[ $dependencies?devDependencies?('ajv-draft-04') ]]",
        "ajv-formats": "[[ $dependencies?devDependencies?('ajv-formats') ]]",
        "cypress": "[[ $dependencies?devDependencies?cypress ]]",
        "cypress-ajv-schema-validator": "[[ $dependencies?devDependencies?('cypress-ajv-schema-validator') ]]",
        "fflate": "[[ $dependencies?devDependencies?fflate ]]",
        "glob": "[[ $dependencies?devDependencies?glob ]]"
    },
    "scripts": {
        "test": "cypress run",
        "test:open": "cypress open"
    }
}